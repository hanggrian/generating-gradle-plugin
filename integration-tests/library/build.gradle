buildscript {
    repositories {
        mavenCentral()
    }
    dependencies {
        classpath files('../buildconfig-gradle-plugin-0.5-rc1.jar')
        classpath 'com.squareup:javapoet:1.11.1'
    }
}

group 'com.example'
version '1.0'

apply plugin: 'java-library'
apply plugin: 'idea'
apply plugin: 'com.hendraanggrian.generating.buildconfig'

dependencies {
    testImplementation 'junit:junit:4.12'
}